'use strict';

/**
 * frontend-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::frontend-image.frontend-image');
